var documento = document

document.appendChild('Modelos nuevos', document.createElement('p'))

const lista_nodos = documento.querySelector('img')

const matriz_nodos = [...lista_nodos]

matriz_nodos.map()